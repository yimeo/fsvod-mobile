# 飞鸿影院移动端界面设计

## 设计目标

飞鸿影院是面向 Android 手机及模拟器的影视聚合浏览器。界面以**深墨蓝影院背景、暖金色强调色与高对比白字**建立沉浸式观影氛围；所有操作遵循 9:16 竖屏、单手可触达的布局，并为 Android 7.0（API 24）及以上系统保留清晰的触摸目标和稳定的回退逻辑。

## 屏幕清单与内容

| 屏幕 | 主要内容 | 关键功能 |
|---|---|---|
| 首页 | 顶部搜索、一级分类、二级子分类、影视双列海报流 | 根据所选分类分页加载、下拉刷新、进入详情 |
| 搜索页 | 搜索输入框、历史关键词、搜索结果海报流 | 按片名搜索、清除关键词、进入详情 |
| 详情页 | 海报、标题、年份、地区、类型、简介、播放线路、剧集按钮 | 选择线路与剧集、启动原生视频播放、收藏播放记录 |
| 播放页 | 视频画面、标题、剧集切换、缓存状态提示 | 使用系统视频控件播放、切换已缓存的剧集地址 |
| 设置页 | 数据源域名、识别状态、已识别接口、缓存管理 | 粘贴域名后自动探测 MACCMS API、保存、清理本地缓存 |

## 关键用户流程

用户首次打开应用时进入首页空状态，点击“配置数据源”进入设置页，填入站点域名并保存。应用依次检测常见 MACCMS JSON API 路径，验证响应结构后记忆可用接口；随后读取一级分类并将带有 `type_pid` 的条目组织为对应一级分类的子分类。用户选择分类后，列表使用分页请求和图片缓存逐屏加载。

用户点击任一影视卡片进入详情页。详情页面将 MACCMS 的 `vod_play_from` 和 `vod_play_url` 解析为播放线路与剧集；第一次查看影片时，解析后的播放列表会写入本地缓存。用户点选剧集即进入播放页，若原始播放地址为常见 HLS、MP4 或可被系统识别的流媒体地址，使用原生视频控件播放；不可直接播放的地址会保留明确提示和复制地址入口。

## 视觉规范

| 视觉元素 | 选型 | 用途 |
|---|---|---|
| 背景色 | `#0B1020` | 全局影院暗色底，减轻暗场观看眩光 |
| 卡片色 | `#151E34` | 海报卡片、设置项与线路容器 |
| 主强调色 | `#F5B64B` | 当前分类、主按钮、播放进度与重点操作 |
| 次强调色 | `#5DB7FF` | 链接、缓存状态和辅助操作 |
| 正文色 | `#F6F7FB` | 标题与核心信息 |
| 次级文字 | `#9CA7BE` | 元数据、提示及空状态说明 |

海报采用 2:3 纵向比例、12px 圆角。加载中以深色骨架屏占位；当数据源没有海报时，应用在设备端即时绘制带有渐变、颗粒感和片名排版的“影视风格文字海报”，不依赖第三方图像生成接口，也不会将片名发送到外部服务。

## 本地数据与兼容性

| 数据 | 存储方式 | 作用 |
|---|---|---|
| 数据源配置与识别结果 | AsyncStorage | 下次启动无需重复填写与探测 |
| 已解析播放列表 | AsyncStorage | 详情页优先即时显示线路与剧集 |
| 最近浏览与搜索词 | AsyncStorage | 支持本地历史与快捷访问 |
| 远程海报缓存 | Expo Image 磁盘缓存 | 减少滚动重复请求及流量 |

应用定位为**播放列表本地缓存**，而非离线下载影视文件。这样可避免长时间后台下载与大文件占用，符合 Android 新旧系统和模拟器的默认存储权限模型。
