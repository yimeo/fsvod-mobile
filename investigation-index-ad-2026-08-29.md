# 官方首页广告位配置检查

检查时间：2026-08-29

已读取的主、备官方配置地址均只包含 `primaryApi`、`backupApi` 与 `sources`，当前未发现 `index_ad` 字段。因此，现阶段无法获知广告脚本地址、返回格式、图片字段、跳转字段或轮播顺序。

广告实现将以 `index_ad` 为单一官方入口，并采用受限的数据解析方式：仅接受明确的广告数据结构（标题、图片、跳转链接、可选展示时长），不直接执行远程 JavaScript 字符串。若 `index_ad` 请求失败、返回格式无效或广告图片无法显示，首页将保留当前 hero 卡片作为兜底。
